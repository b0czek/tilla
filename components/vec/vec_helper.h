#pragma once

/**
 * 
 * @brief vec_push macro returns (result, 0) for some reason so it is necessary 
 * to use function like this
 * @param r 
 * @param x 
 * @return int result of push
 */
int vec_push_check(int r, int x);